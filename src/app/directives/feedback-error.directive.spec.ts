import { FeedbackErrorDirective } from './feedback-error.directive';

describe('FeedbackErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new FeedbackErrorDirective();
    expect(directive).toBeTruthy();
  });
});
