import { SortAndFilterPipe } from './sort-and-filter.pipe';

describe('SortAndFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAndFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
