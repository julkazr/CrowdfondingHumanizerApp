// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyBpL8RtgC0mGL9Qs6APUVbpatw1B0JdjH0',
    authDomain: 'humaniser-25b85.firebaseapp.com',
    databaseURL: 'https://humaniser-25b85.firebaseio.com',
    projectId: 'humaniser-25b85',
    storageBucket: 'humaniser-25b85.appspot.com',
    messagingSenderId: '1085081010805'
  }
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
